{
  "name": "favorite-widget",
  "version": "0.2.0",
  "description": "Windows desktop favorite widget MVP in C++/Win32",
  "private": true,
  "main": "build/favorite_widget.exe",
  "scripts": {
    "configure": "cmake -S . -B build -G \"MinGW Makefiles\"",
    "build": "cmake --build build",
    "settings:build": "dotnet build settings-ui/FavoriteWidget.Settings/FavoriteWidget.Settings.csproj -c Release -p:Platform=x64",
    "settings:msix": "dotnet build settings-ui/FavoriteWidget.Settings/FavoriteWidget.Settings.csproj -c Release -p:Platform=x64 -p:GenerateAppxPackageOnBuild=true -p:AppxBundle=Never -p:AppxPackageSigningEnabled=false -p:AppxPackageDir=dist/settings/",
    "check": "npm run configure && npm run build",
    "installer": "npm run configure && npm run build && cpack --config build/CPackConfig.cmake -G NSIS64 -B dist",
    "package": "npm run configure && npm run build && cpack --config build/CPackConfig.cmake -B dist",
    "site:preview": "python -m http.server 4173 --directory docs",
    "start": "npm run configure && npm run build && .\\build\\favorite_widget.exe",
    "dev": "npm run start",
    "smoke": "npm run configure && npm run build && .\\build\\favorite_widget.exe --smoke-test",
    "test:crop": "python tools/test/crop_screenshots.py",
    "test:docker": "docker build -f tools/test/Dockerfile -t favorite-widget-test . && docker run --rm favorite-widget-test && docker image rm favorite-widget-test",
    "test:pages:docker": "powershell -ExecutionPolicy Bypass -File tools/test/run_pages_docker.ps1"
  }
}
